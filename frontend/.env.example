# Port à interroger côté backend Rust (aligné sur backend/.env : PORT=60005).
# Le proxy Nuxt (server/api/*) et le WebSocket utilisent NUXT_HPCCAT_API_PORT
# via runtimeConfig (nuxt.config.ts).
NUXT_HPCCAT_API_PORT=60005